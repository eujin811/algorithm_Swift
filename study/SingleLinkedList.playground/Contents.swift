import UIKit

/*
 SIngleLinkedList 단일 연결리스트
 하나의 포인터 멤버로 다른 노드 데이터를 가르키는 것
 HEAD 없이 TAIL만 존재
 앞으로 돌아갈 수 없다. 중간위치 바로 접근 불가능 해서 Head 노드의 주소를 잃어버리면 데이터 접근 불가능
 
 */

var str = "Single List"

protocol LinkedListStack {
    
}
